<UserControl x:Class="CorsairDashboard.Views.CyclingColorLedView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>
        
        <StackPanel Orientation="Horizontal">
            <TextBlock Text="Number of colors: " VerticalAlignment="Center"></TextBlock>
            <ComboBox x:Name="NumberOfColors"></ComboBox>
        </StackPanel>

        <StackPanel Orientation="Horizontal" Grid.Row="1" Margin="0, 10">
            <ContentControl x:Name="FirstColorChooser"></ContentControl>
            <ContentControl x:Name="SecondColorChooser"></ContentControl>
            <ContentControl x:Name="ThirdColorChooser"></ContentControl>
            <ContentControl x:Name="FourthColorChooser"></ContentControl>
        </StackPanel>               
    </Grid>
</UserControl>
